{
  "name": "yield-ui",
  "packageManager": "yarn@3.1.1",
  "private": true,
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "clean": "lerna clean --yes",
    "document": "typedoc" ,
    "lint": "yarn run eslint --ext .ts,.tsx .",
    "test": "lerna run test",
    "build": "lerna run build",
    "prestart": "yarn build",
    "prepack": "yarn build",
    "compile": "lerna run compile",
    "startdev": "lerna run watch"
  },
  "dependencies": {
    "@yield-protocol/ui-core": "1.0.0",
    "@yield-protocol/ui-react": "1.0.0",
    "@yield-protocol/ui-math": "1.0.0",
    "@yield-protocol/ui-cli": "1.0.0"
  },
  "devDependencies": {
    "esbuild-serve": "^1.0.1",
    "lerna": "^4.0.0",
    "typedoc": "^0.22.17"
  },
  "workspaces": [
    "packages/*",
    "examples/*"
  ]
}
